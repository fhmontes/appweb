{{> header }}
<main role="main" class="container">
    <h1>{{ titulo }}</h1>
    <table class="table">
        <tr>
            <th>ID</th>
            <th>NOMBRE</th>
        </tr>
        {{#each personas }}
            <tr>
                <td>{{ this.id }}</td>
                <td>{{ this.nombre }}</td>
            </tr>
        {{/each}}
    </table>
</main>
{{> footer }}